[pytest]
# Allows `pytest` to run from the repository root (see .github/copilot-instructions.md).
# The backend/pyproject.toml config still applies when running inside backend/.
asyncio_mode = auto
pythonpath = backend
testpaths = backend/tests
markers =
    db: test requires a live PostgreSQL database (TEST_DATABASE_URL)
